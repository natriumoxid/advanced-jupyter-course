# advanced-jupyter-course
A lot of Jupyter Examples and course material

# Prerequistes
pip install jupyterlab
pip install numpy matplotlib scipy pandas
pip install psycopg2
pip install geos
pip install descartes
pip install proj
pip install geopandas
pip install geoplot